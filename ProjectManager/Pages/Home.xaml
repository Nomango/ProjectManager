<UserControl x:Class="ProjectManager.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI" xmlns:pages="clr-namespace:ProjectManager.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!--<ScrollViewer>
        <StackPanel MinWidth="200">
            <ProgressBar x:Name="Progress" Visibility="Hidden" Minimum="0" Maximum="1" Height="16" Margin="0,0,0,5" />
            <mui:ModernMenu x:Name="ShareList" Margin="0,0,0,16" SelectedSource="/link1">
            </mui:ModernMenu>
            <TreeView x:Name="DirTree" />
        </StackPanel>
    </ScrollViewer>-->
    <!--<Grid Style="{StaticResource ContentRoot}">
        <ProgressBar x:Name="Progress" Visibility="Hidden" Minimum="0" Maximum="1" Height="16" Margin="0,0,0,5" />
        <mui:ModernTab x:Name="ShareList" Layout="Tab">
            <mui:ModernTab.Links>
                <mui:Link DisplayName="Basic" Source="/Content/LoremIpsum.xaml" />
                <mui:Link DisplayName="Split" Source="/Content/LoremIpsumSplit.xaml" />
                <mui:Link DisplayName="List" Source="/Content/LoremIpsumList.xaml" />
            </mui:ModernTab.Links>
        </mui:ModernTab>
    </Grid>-->
    <!--<mui:ModernTab SelectedSource="/Pages/FileList.xaml" Layout="List">
        <mui:ModernTab.Links>
            <mui:Link DisplayName="My Files" Source="/Pages/FileList.xaml" />
        </mui:ModernTab.Links>
    </mui:ModernTab>-->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="ListShareFolder"
                 ItemsSource="{Binding ShareNames}"
                 SelectedItem="{Binding CurrentShareName}"
                 Margin="{StaticResource SplitLeft}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Width="160" Padding="16,4,32,4" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <GridSplitter Grid.Column="1" />
        <pages:FileList x:Name="FileList" Grid.Column="2" Margin="{StaticResource SplitRight}" />
    </Grid>
</UserControl>
